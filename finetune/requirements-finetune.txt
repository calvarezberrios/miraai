# LOCAL data-prep deps only (CPU, no GPU needed).
#
# The actual training runs on a free Google Colab GPU via mira_finetune_colab.ipynb,
# which pip-installs its own GPU stack (Unsloth + torch + trl + peft + bitsandbytes)
# inside Colab. You do NOT install any of that on your 6 GB machine.
#
# These two are only needed if you want to generate / inspect the training JSONL
# locally with prepare_data.py (optional — the notebook does the same prep itself):
#
#   python -m pip install -r requirements-finetune.txt
#   python prepare_data.py --out data/persona_chat.jsonl --max-rows 50

datasets
huggingface_hub
